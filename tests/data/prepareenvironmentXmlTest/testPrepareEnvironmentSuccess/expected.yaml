# Local Doctrine configuration
doctrine:

    # Default ORM connection
    connection:
        orm_default:
            params:
                host: "testhost"
                port: "testport"
                user: "testuser"
                password: "testpass"
                dbname: "testdb"
